# Copy to .env and adjust. .env is gitignored and must never hold committed secrets.
REDIS_HOST=localhost
REDIS_PORT=6383
REDIS_DB=0
# REDIS_PASSWORD=
APP_ENV=dev
KEY_PREFIX=rdp

# Used by the CLI (cli/main.py)
RDP_API_URL=http://localhost:8800
